[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && python railway_main.py"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"

[variables]
PYTHONPATH = "/app/backend"
PORT = "8000"