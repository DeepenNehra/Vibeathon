# ============================================
# AROGYA-AI BACKEND ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env and fill in your actual values
# Never commit .env to version control!

# ============================================
# LIVE CAPTIONS CONFIGURATION
# ============================================
# For detailed setup instructions, see: CAPTIONS_ENV_SETUP.md

# Google Cloud Credentials (REQUIRED for Live Captions)
# Path to your Google Cloud service account JSON file
# Get this from: Google Cloud Console > IAM & Admin > Service Accounts
# Required APIs: Cloud Speech-to-Text, Cloud Translation
# Example: ./google-credentials.json or /path/to/google-credentials.json
GOOGLE_APPLICATION_CREDENTIALS=path/to/google-credentials.json

# OpenAI API Key (OPTIONAL - Fallback ASR for Live Captions)
# Used when Google Cloud STT fails or is unavailable
# Get this from: https://platform.openai.com/api-keys
# Format: sk-proj-... or sk-...
# Free tier: No free tier, pay-per-use ($0.006/minute)
# Note: This is optional but recommended for reliability
OPENAI_API_KEY=your_openai_api_key

# ============================================
# DATABASE CONFIGURATION
# ============================================

# Supabase Configuration (REQUIRED for main app features)
# Get these from: Supabase Dashboard > Project Settings > API
# Use the service_role key (NOT the anon key) for backend
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here

# PostgreSQL Database URL (OPTIONAL - For transcript storage)
# Format: postgresql://user:password@host:port/database
# Example: postgresql://postgres:password@localhost:5432/arogya_ai
# Note: If using Supabase, you can get this from Database Settings
DATABASE_URL=postgresql://user:password@host:port/database

# ============================================
# AI SERVICES CONFIGURATION
# ============================================

# Google Gemini API Key (REQUIRED for SOAP note generation)
# Get this from: https://makersuite.google.com/app/apikey
# Free tier: 60 requests per minute
GEMINI_API_KEY=your_gemini_api_key

# ============================================
# CORS CONFIGURATION
# ============================================

# Frontend URL (for CORS)
# Development: http://localhost:3000
# Production: https://your-domain.com
FRONTEND_URL=http://localhost:3000

# ============================================
# OPTIONAL CONFIGURATION
# ============================================

# Google Cloud Project ID (OPTIONAL)
# Only needed if you want to explicitly set the project
# Usually auto-detected from credentials file
# GOOGLE_CLOUD_PROJECT_ID=your-project-id

# Log Level (OPTIONAL)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
# LOG_LEVEL=INFO

# ============================================
# API QUOTA LIMITS (For Reference)
# ============================================
# These are not environment variables, just reference information

# Google Cloud Speech-to-Text:
# - Free tier: 60 minutes/month
# - Paid: $0.006 per 15 seconds (standard), $0.009 (enhanced)

# Google Cloud Translation:
# - Free tier: 500,000 characters/month
# - Paid: $20 per 1 million characters

# OpenAI Whisper API:
# - No free tier
# - $0.006 per minute

# Google Gemini:
# - Free tier: 60 requests per minute
# - Check current limits at: https://ai.google.dev/pricing

# ============================================
# SETUP INSTRUCTIONS
# ============================================
# 1. Copy this file: cp .env.example .env
# 2. Fill in your actual API keys and credentials
# 3. For live captions setup, see: CAPTIONS_ENV_SETUP.md
# 4. Never commit .env to git (it's in .gitignore)
# 5. Use different credentials for dev/staging/production

# ============================================
# TROUBLESHOOTING
# ============================================
# If you encounter issues:
# 1. Check CAPTIONS_ENV_SETUP.md for detailed troubleshooting
# 2. Verify all required APIs are enabled in Google Cloud Console
# 3. Ensure credentials file exists and has correct permissions
# 4. Check API quotas haven't been exceeded
# 5. Verify FFmpeg is installed (required for audio conversion)
